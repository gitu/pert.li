node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
mcp-todos.json
.data
storybook-static

# IDE
.idea/
.vscode/

# Playwright e2e outputs
test-results/
playwright-report/
blob-report/
playwright/.cache/
e2e/.auth/

# Local debugging artifacts
.playwright-mcp/
*.png
!docs/screenshots/*.png
debug-*.log

# This project is pnpm-only. If npm/yarn ever produce a lockfile here it's
# a tooling misconfiguration (most commonly Dependabot guessing wrong when
# `packageManager` isn't declared in package.json).
package-lock.json
yarn.lock
