-- Dev seed script run by the neon vite plugin on first provision.
-- Schema is owned by Drizzle (src/db/schema.ts); run `pnpm db:push` after
-- the first `pnpm dev` to create the Better Auth tables in the fresh database.
-- Keep this file for future bootstrap data (e.g. demo workspace once it exists).

SELECT 1;
